{
  "name": "deskline",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm.cmd run dev --prefix backend\" \"npm.cmd run dev --prefix frontend\"",
    "dev:backend": "npm.cmd run dev --prefix backend",
    "dev:frontend": "npm.cmd run dev --prefix frontend",
    "build": "npm.cmd run build --prefix backend && npm.cmd run build --prefix frontend",
    "build:unix": "npm run build --prefix backend && npm run build --prefix frontend",
    "start": "npm.cmd run start --prefix backend",
    "start:unix": "npm run start --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
